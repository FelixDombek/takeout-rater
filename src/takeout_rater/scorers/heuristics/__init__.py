"""Heuristic scorers package.

Heuristic scorers derive scores from image properties (blur, exposure,
perceptual hash, …) without requiring an ML model.  They are always
available as long as a basic image library (e.g. Pillow) is installed.
"""
