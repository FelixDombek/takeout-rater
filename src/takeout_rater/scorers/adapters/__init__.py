"""Adapter scorers package.

Adapters wrap external tools or heavyweight ML pipelines and expose them
through the :class:`~takeout_rater.scorers.base.BaseScorer` interface.
Heavy optional dependencies are declared as extras in ``pyproject.toml``.
"""
